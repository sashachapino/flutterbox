version: '3.8'

services:
  flutterbox:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=false
      - PORT=5000
    volumes:
      - ./static:/app/static
      - ./templates:/app/templates
